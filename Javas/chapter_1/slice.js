// Slice , the first argument tells from where we have to slice and the second tell to where it could b eslice


let email = 'anasbakkar.official@outlook.com';
let resul = email.slice(0,5);
console.log(resul);